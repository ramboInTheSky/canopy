{
  "name": "@_alz/boxshadow-style",
  "version": "0.1.0-beta.12",
  "description": "A function that takes a theme and an elevation, calculates the opacity, and returns the correct box shadow",
  "main": "./lib/dist/index.js",
  "module": "./lib/esm/index.js",
  "types": "./lib/ts/index.ts",
  "author": "Alessio Fimognari <alefimo@gmail.com>",
  "scripts": {
    "build": "gulp build",
    "watch": "gulp watch",
    "clean": "rm -rf ./lib/ && rm -f package-lock.json",
    "lint": "gulp lint"
  },
  "babel": {
    "presets": [
      "module:@_alz/babel-preset"
    ]
  },
  "files": [
    "lib/**/*"
  ],
  "dependencies": {
    "@babel/core": "7.0.0-beta.47"
  },
  "devDependencies": {
    "@_alz/babel-preset": "^0.1.0-beta.12",
    "@_alz/themes": "^0.1.0-beta.12",
    "@_alz/tslint-config": "^0.1.0-beta.12"
  }
}
